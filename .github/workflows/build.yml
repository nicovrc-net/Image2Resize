# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-gradle

name: Java CI with Gradle

on:
  push:
    branches: [ "master", "v1" ]
    tags: ['*']
  pull_request:
    branches: [ "master", "v1" ]

jobs:
  build-21:
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'oracle'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6.1.0

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build with Gradle Wrapper
        run: ./gradlew shadowJar

      - name: Upload JarFile
        uses: actions/upload-artifact@v7.0.0
        with:
          name: jar
          path: ./build/libs/Image2Resize-1.0-SNAPSHOT-all.jar

  build-26:
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up JDK 26
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '26'
          distribution: 'oracle'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6.1.0

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build with Gradle Wrapper
        run: ./gradlew shadowJar

      - name: Upload JarFile
        uses: actions/upload-artifact@v7.0.0
        with:
          name: jar24
          path: ./build/libs/Image2Resize-1.0-SNAPSHOT-all.jar

  download_ffmpeg:
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - name: Download ffmpeg
        run: |
          wget https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-master-latest-linux64-gpl.tar.xz > null
          tar -Jxvf ./ffmpeg-master-latest-linux64-gpl.tar.xz > null
          cp ./ffmpeg-master-latest-linux64-gpl/bin/* ./
          rm ./ffmpeg-master-latest-linux64-gpl.tar.xz
          rm ./ffmpeg-master-latest-linux64-gpl -fr
        shell: bash

      - name: Upload ffmpeg
        uses: actions/upload-artifact@v7.0.0
        with:
          name: ffmpeg
          path: ./ffmpeg

  autotest1:
    needs: [build-21]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Download JarFile
        uses: actions/download-artifact@v8.0.1
        with:
          name: jar

      - name: Run the application
        run: java -jar ./Image2Resize-1.0-SNAPSHOT-all.jar &
        shell: bash

      - name: Wait for the application to start
        run: sleep 2

      - name: Create config.yml Test
        run: |
          if [ -f ./config.yml ]; then
            echo "OK"
          else 
            exit 1
          fi
        shell: bash

  autotest2:
    needs: [build-21]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Download JarFile
        uses: actions/download-artifact@v8.0.1
        with:
          name: jar

      - name: Run the application
        run: java -jar ./Image2Resize-1.0-SNAPSHOT-all.jar --default-config-mode &
        shell: bash

      - name: Wait for the application to start
        run: sleep 2

      - name: Create LogFolder Test
        run: |
          if [ -d ./log ]; then
            echo "OK"
          else
            exit 1
          fi
        shell: bash

  autotest3:
    needs: [build-21, download_ffmpeg]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Download JarFile
        uses: actions/download-artifact@v8.0.1
        with:
          name: jar

      - name: Download ffmpeg
        uses: actions/download-artifact@v8.0.1
        with:
          name: ffmpeg

      - name: Make ffmpeg executable
        run: chmod +x ./ffmpeg

      - name: Run the application
        run: java -jar ./Image2Resize-1.0-SNAPSHOT-all.jar --default-config-mode &
        shell: bash

      - name: Wait for the application to start
        run: sleep 2

      - name: URL Resize Test
        run: |
          HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" http://localhost:25555/?url=https://raw.githubusercontent.com/nicovrc-net/Image2Resize/refs/heads/master/.github/workflows/i2i_autotest.png)
          if [ "$HTTP_STATUS" -ne 200 ]; then
            echo "Expected HTTP status 200 but got $HTTP_STATUS"
            exit 1
          fi
        shell: bash

  autotest4:
    needs: [build-21, download_ffmpeg]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Download JarFile
        uses: actions/download-artifact@v8.0.1
        with:
          name: jar

      - name: Download ffmpeg
        uses: actions/download-artifact@v8.0.1
        with:
          name: ffmpeg

      - name: Make ffmpeg executable
        run: chmod +x ./ffmpeg

      - name: Run the application
        run: java -jar ./Image2Resize-1.0-SNAPSHOT-all.jar --default-config-mode &
        shell: bash

      - name: Wait for the application to start
        run: sleep 2

      - name: API Check Test 1
        run: |
          HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" http://localhost:25555/api/v1/get_data)
          if [ "$HTTP_STATUS" -ne 200 ]; then
            echo "Expected HTTP status 200 but got $HTTP_STATUS"
            exit 1
          fi
        shell: bash

  autotest5:
    needs: [build-21, download_ffmpeg]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Download JarFile
        uses: actions/download-artifact@v8.0.1
        with:
          name: jar

      - name: Download ffmpeg
        uses: actions/download-artifact@v8.0.1
        with:
          name: ffmpeg

      - name: Make ffmpeg executable
        run: chmod +x ./ffmpeg

      - name: Download JsonFile
        run: wget https://raw.githubusercontent.com/nicovrc-net/Image2Resize/refs/heads/master/.github/workflows/testfile.json
        shell: bash

      - name: Run the application
        run: java -jar ./Image2Resize-1.0-SNAPSHOT-all.jar --default-config-mode &
        shell: bash

      - name: Wait for the application to start
        run: sleep 2

      - name: API Check Test 2-1
        run: |
          HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" -X POST -H "Content-Type: application/json" -d @testfile.json http://localhost:25555/api/v1/image_resize)
          if [ "$HTTP_STATUS" -ne 200 ]; then
            echo "Expected HTTP status 200 but got $HTTP_STATUS"
            exit 1
          fi
        shell: bash

  autotest6:
    needs: [build-21, download_ffmpeg]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Download JarFile
        uses: actions/download-artifact@v8.0.1
        with:
          name: jar

      - name: Download ffmpeg
        uses: actions/download-artifact@v8.0.1
        with:
          name: ffmpeg

      - name: Make ffmpeg executable
        run: chmod +x ./ffmpeg

      - name: Run the application
        run: java -jar ./Image2Resize-1.0-SNAPSHOT-all.jar --default-config-mode &
        shell: bash

      - name: Wait for the application to start
        run: sleep 2

      - name: API Check Test 2-2
        run: |
          HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" -X POST -H "Content-Type: application/json" -d '{"filename":"autotest.txt", "content":"44Gm44GN44GZ44Go44Gg44KI44O844KT"}' http://localhost:25555/api/v1/image_resize)
          if [ "$HTTP_STATUS" -ne 404 ]; then
            exit 1
          fi
        shell: bash

  autotest7:
    needs: [build-21]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Download JarFile
        uses: actions/download-artifact@v8.0.1
        with:
          name: jar

      - name: Run the application
        run: java -jar ./Image2Resize-1.0-SNAPSHOT-all.jar --default-config-mode &
        shell: bash

      - name: Wait for the application to start
        run: sleep 2

      - name: Create stop.txt
        run: |
          cat /dev/null > stop.txt
        shell: bash

      - name: Wait 2s
        run: sleep 2

      - name: Force Stop Test
        run: |
          if [ -d ./stop.txt ]; then
            exit 1
          else
            echo "OK"
          fi
        shell: bash

  autotest8:
    needs: [build-21]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Download JarFile
        uses: actions/download-artifact@v8.0.1
        with:
          name: jar

      - name: Run the application
        run: java -jar ./Image2Resize-1.0-SNAPSHOT-all.jar --default-config-mode &
        shell: bash

      - name: Wait for the application to start
        run: sleep 2

      - name: Access
        run: |
          curl http://localhost:25555/api/v1/test > /dev/null
          curl http://localhost:25555/api/v1/test > /dev/null
          curl http://localhost:25555/api/v1/test > /dev/null
          curl http://localhost:25555/api/v1/test > /dev/null
          curl http://localhost:25555/api/v1/test > /dev/null
        shell: bash

      - name: Create stop.txt
        run: |
          cat /dev/null > stop.txt
        shell: bash

      - name: Wait 2s
        run: sleep 2

      - name: LogWrite Test
        run: |
          COUNT=$(ls -U1 ./log | wc -l)
          if [ "$COUNT" = "5" ]; then
            echo "OK"
          else
            echo "$COUNT <> 5"
            ls -U1 ./log
            exit 1
          fi
        shell: bash

  autotest9:
    needs: [build-21, download_ffmpeg]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Download JarFile
        uses: actions/download-artifact@v8.0.1
        with:
          name: jar

      - name: Download ffmpeg
        uses: actions/download-artifact@v8.0.1
        with:
          name: ffmpeg

      - name: Make ffmpeg executable
        run: chmod +x ./ffmpeg

      - name: Run the application
        run: java -jar ./Image2Resize-1.0-SNAPSHOT-all.jar --default-config-mode &
        shell: bash

      - name: Wait for the application to start
        run: sleep 2

      - name: Access
        run: |
          curl http://localhost:25555/?url=https://raw.githubusercontent.com/nicovrc-net/Image2Resize/refs/heads/master/.github/workflows/i2i_autotest.png > /dev/null
        shell: bash

      - name: Create stop.txt
        run: |
          cat /dev/null > stop.txt
        shell: bash

      - name: Wait 2s
        run: sleep 2

      - name: CacheFolder IsEmpty Test
        run: |
          COUNT=$(ls -U1 ./cache | wc -l)
          if [ "$COUNT" = "0" ]; then
            echo "OK"
          else
            echo "$COUNT <> 0"
            exit 1
          fi
        shell: bash

  autotest10:
    needs: [build-21, download_ffmpeg]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Download JarFile
        uses: actions/download-artifact@v8.0.1
        with:
          name: jar

      - name: Download ffmpeg
        uses: actions/download-artifact@v8.0.1
        with:
          name: ffmpeg

      - name: Make ffmpeg executable
        run: chmod +x ./ffmpeg

      - name: Run the application
        run: java -jar ./Image2Resize-1.0-SNAPSHOT-all.jar --default-config-mode &
        shell: bash

      - name: Wait for the application to start
        run: sleep 2

      - name: Site OGPTag Check Test 1
        run: |
          HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" http://localhost:25555/?url=https://www.nicovideo.jp/watch/sm500873)
          if [ "$HTTP_STATUS" -ne 200 ]; then
            exit 1
          else
            echo "OK"
          fi
        shell: bash

  autotest11:
    needs: [build-21, download_ffmpeg]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Download JarFile
        uses: actions/download-artifact@v8.0.1
        with:
          name: jar

      - name: Download ffmpeg
        uses: actions/download-artifact@v8.0.1
        with:
          name: ffmpeg

      - name: Make ffmpeg executable
        run: chmod +x ./ffmpeg

      - name: Run the application
        run: java -jar ./Image2Resize-1.0-SNAPSHOT-all.jar --default-config-mode &
        shell: bash

      - name: Wait for the application to start
        run: sleep 2

      - name: Site OGPTag Check Test 3
        run: |
          HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" http://localhost:25555/?url=https://vrchat-fbt.com/category/寄付者/茅野ななみ/)
          if [ "$HTTP_STATUS" -ne 200 ]; then
            exit 1
          else
            echo "OK"
          fi
        shell: bash

  autotest12:
    needs: [build-21, download_ffmpeg]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Download JarFile
        uses: actions/download-artifact@v8.0.1
        with:
          name: jar

      - name: Download ffmpeg
        uses: actions/download-artifact@v8.0.1
        with:
          name: ffmpeg

      - name: Make ffmpeg executable
        run: chmod +x ./ffmpeg

      - name: Run the application
        run: java -jar ./Image2Resize-1.0-SNAPSHOT-all.jar --default-config-mode &
        shell: bash

      - name: Wait for the application to start
        run: sleep 2

      - name: Site OGPTag Check Test 3
        run: |
          HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" http://localhost:25555/?url=https://www.google.com/)
          if [ "$HTTP_STATUS" -ne 404 ]; then
            exit 1
          else
            echo "OK"
          fi
        shell: bash
  release:
    needs: [autotest1, autotest2, autotest3, autotest4, autotest5, autotest6, autotest7, autotest8, autotest9, autotest10, autotest11, autotest12]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - name: Download JarFile
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/download-artifact@v8.0.1
        with:
          name: jar
      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        id: create_release
        uses: actions/create-release@latest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: v${{ github.ref }}
          body: |
            - 内部リファクタリング
          draft: false
          prerelease: false

      - name: Upload JarFile
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./Image2Resize-1.0-SNAPSHOT-all.jar
          asset_name: Image2Resize-1.0-SNAPSHOT-all.jar
          asset_content_type: application/java-archive

      - name: Delete File
        uses: geekyeggo/delete-artifact@v5
        with:
          name: |
            jar
            ffmpeg